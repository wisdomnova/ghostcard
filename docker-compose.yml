version: '3.8'

services:
  ghostcard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ghostcard-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
